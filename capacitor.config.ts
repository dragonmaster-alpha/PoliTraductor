import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aklame.politraductor',
  appName: 'PoliTraductor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
